extends layout

block content

  if (!user)
    div.accueil
      span.letter.letterd D 
      span.letter.letterr r
      span.letter.lettera a 
      span.letter.letterw w  
      img.pen(src='/img/pen.png' alt="pen")
  if (user)
    div.boxMenu
      a.menu(href='/game') Solo
      a.menu(href='/game') Multijoueurs
      a.menu(href='/game') Scores
    