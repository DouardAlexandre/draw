extends layout

block content

  if (!user)
    div.accueil
      span.letter D 
      span.letter r
      span.letter a 
      span.letter w  
      img.pen(src='/img/pen.png' alt="pen")
  if (user)
    div.boxMenu
      a.menu(href='/game') Solo
      a.menu(href='#') Multijoueurs
      a.menu(href='#') Scores
      

 
	

  