extends layout

block content
  div.container.register
    form.form-signin(role='form', action="/register",method="post")
      h2.form-signin-heading S'enregistrer
      input.form-control(type='text', name="name", placeholder='Nom')
      input.form-control(type='text', name="username", placeholder='nom joueur')
      input.form-control(type='password', name="password", placeholder='Mot de passe')
      button.btn.btn-lg.btn-primary.btn-block(type='submit') ok